import { Logger } from '../ui';
import { GraphQLRunner } from './graphql-client';
export declare function stringify(variables: any): string;
export declare class DebuggingGraphqlRunner implements GraphQLRunner {
    private readonly innerClient;
    private readonly endpoint;
    private readonly logger;
    constructor(innerClient: GraphQLRunner, endpoint: string, logger: Logger);
    run<TVariables, TResponse>(query: string, variables: TVariables): Promise<{
        requestId: string | undefined;
        response: TResponse;
    }>;
}
//# sourceMappingURL=debugging-graphql-runner.d.ts.map