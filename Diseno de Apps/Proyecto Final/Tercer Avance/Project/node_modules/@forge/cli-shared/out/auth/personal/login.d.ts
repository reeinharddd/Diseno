import { GraphQLClient, User } from '../../graphql';
import { Logger } from '../../ui';
import { Authenticator } from '../../auth';
import { PersonalApiCredentials, PersonalApiCredentialsStore, PersonalApiCredentialsValidated } from './token-store';
export declare class LoginCommand {
    private readonly createGraphQLClient;
    private readonly store;
    private readonly logger;
    constructor(createGraphQLClient: (authenticator: Authenticator) => GraphQLClient, store: PersonalApiCredentialsStore, logger: Logger);
    execute(credentials: PersonalApiCredentials): Promise<{
        user: User;
        creds: PersonalApiCredentialsValidated;
        analytics: {
            userId: string;
        };
    }>;
    getUser(credentials: PersonalApiCredentials): Promise<User>;
}
//# sourceMappingURL=login.d.ts.map