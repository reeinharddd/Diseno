"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class CreateContainerTokenCommand {
    constructor(graphQLClient, getConfiguredApp) {
        this.graphQLClient = graphQLClient;
        this.getConfiguredApp = getConfiguredApp;
    }
    async execute() {
        const { id: appId } = await this.getConfiguredApp();
        const token = await this.graphQLClient.createToken({ appId });
        return token;
    }
}
exports.CreateContainerTokenCommand = CreateContainerTokenCommand;
