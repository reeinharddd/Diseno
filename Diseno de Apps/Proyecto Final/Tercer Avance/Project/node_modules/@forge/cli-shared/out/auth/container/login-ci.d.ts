import { AppConfigProvider } from '../../apps';
export interface AuthAppDetails {
    appId: string;
}
export interface ContainerTokenGraphqlClient {
    createToken(details: AuthAppDetails): Promise<string>;
}
export declare class CreateContainerTokenCommand {
    private readonly graphQLClient;
    private readonly getConfiguredApp;
    constructor(graphQLClient: ContainerTokenGraphqlClient, getConfiguredApp: AppConfigProvider);
    execute(): Promise<string>;
}
//# sourceMappingURL=login-ci.d.ts.map