"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const ui_1 = require("../ui");
const ui_2 = require("../ui");
function trimInstallationId(installationId, throwError) {
    const trimmedId = installationId.trim();
    if (throwError && trimmedId.length === 0) {
        throw new ui_2.ValidationError(ui_1.Text.installationId.errors.invalid);
    }
    return trimmedId;
}
async function getInstallationId(ui, installationId) {
    const trimmedId = trimInstallationId(installationId || '', false);
    if (trimmedId.length > 0) {
        return trimmedId;
    }
    const promptedId = await ui.promptForText(ui_1.Text.installationId.prompt);
    return trimInstallationId(promptedId, true);
}
exports.getInstallationId = getInstallationId;
