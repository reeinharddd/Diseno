import { FileSystemReader } from './file-system-reader';
export interface FileSystemWatcher {
    watchForChanges(relativePath: string, onChange: () => void): void;
    stopWatching(): void;
}
export declare class FileSystemWithIgnoresWatcher implements FileSystemWatcher {
    private readonly ignorePatterns;
    private fsWatcher;
    constructor(fileSystemReader: FileSystemReader);
    watchForChanges(relativePath: string, onChange: () => void): void;
    stopWatching(): void;
    private fileIsIgnored;
}
//# sourceMappingURL=file-system-watcher.d.ts.map