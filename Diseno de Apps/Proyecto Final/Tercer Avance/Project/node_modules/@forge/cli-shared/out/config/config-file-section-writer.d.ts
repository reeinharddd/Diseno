import { ConfigWriter } from './';
import { Codec, CodecType, InnerType } from './config-codec-types';
import { ConfigSectionWriter } from './config-section';
export declare class ConfigFileSectionWriter<K extends string, V extends object> implements ConfigSectionWriter<V> {
    private readonly key;
    private readonly configWriter;
    constructor(key: K, configWriter: ConfigWriter);
    writeConfigSection(configSection: V): Promise<void>;
}
export declare const configFileWriterForSection: <K extends string, P extends Codec>(key: K, schema: CodecType<P>, configWriter: ConfigWriter) => ConfigSectionWriter<InnerType<P>>;
//# sourceMappingURL=config-file-section-writer.d.ts.map