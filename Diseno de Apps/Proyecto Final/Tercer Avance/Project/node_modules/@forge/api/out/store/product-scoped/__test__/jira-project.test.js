"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const jira_project_1 = require("../jira-project");
const product_scoped_storage_1 = require("../product-scoped-storage");
let jiraProjectStorage;
describe('JiraIssueStorage', () => {
    beforeEach(() => {
        jiraProjectStorage = new jira_project_1.JiraProjectStorage('test123', {});
    });
    it('should extend ProductScopedStorage class', () => {
        expect(jiraProjectStorage).toBeInstanceOf(product_scoped_storage_1.ProductScopedStorage);
    });
    it('should correctly build the API path based on the context passed to its constructor', () => {
        expect(jiraProjectStorage['storageApiPath']).toEqual('/rest/api/3/project/test123/properties');
    });
});
