"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const jira_issue_1 = require("../jira-issue");
const product_scoped_storage_1 = require("../product-scoped-storage");
let jiraIssueStorage;
describe('JiraIssueStorage', () => {
    beforeEach(() => {
        jiraIssueStorage = new jira_issue_1.JiraIssueStorage('test123', {});
    });
    it('should extend ProductScopedStorage class', () => {
        expect(jiraIssueStorage).toBeInstanceOf(product_scoped_storage_1.ProductScopedStorage);
    });
    it('should correctly build the API path based on the context passed to its constructor', () => {
        expect(jiraIssueStorage['storageApiPath']).toEqual('/rest/api/3/issue/test123/properties');
    });
});
