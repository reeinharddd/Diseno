"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
global.api = {
    asApp() {
        return {
            __requestAtlassian() {
                return {};
            }
        };
    }
};
const experimental_1 = require("../experimental");
describe('experimental api', () => {
    it('store API has properties from both the global storage API and the experimental API', () => {
        expect(experimental_1.store).toHaveProperty('get');
        expect(experimental_1.store).toHaveProperty('set');
        expect(experimental_1.store).toHaveProperty('delete');
        expect(experimental_1.store).toHaveProperty('query');
        expect(experimental_1.store).toHaveProperty('onJiraIssue');
        expect(experimental_1.store).toHaveProperty('onJiraProject');
        expect(experimental_1.store).toHaveProperty('onConfluencePage');
        expect(experimental_1.store).toHaveProperty('onConfluenceSpace');
    });
});
