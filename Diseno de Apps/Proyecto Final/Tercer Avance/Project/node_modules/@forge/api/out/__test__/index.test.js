"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
global.api = {
    asApp() {
        return {
            __requestAtlassian() {
                return {};
            }
        };
    }
};
const index_1 = require("../index");
describe('storage api', () => {
    it('storage API has KV methods', () => {
        expect(index_1.storage).toHaveProperty('get');
        expect(index_1.storage).toHaveProperty('set');
        expect(index_1.storage).toHaveProperty('delete');
        expect(index_1.storage).toHaveProperty('query');
        expect(index_1.storage).not.toHaveProperty('onJiraIssue');
        expect(index_1.storage).not.toHaveProperty('onJiraProject');
        expect(index_1.storage).not.toHaveProperty('onConfluencePage');
        expect(index_1.storage).not.toHaveProperty('onConfluenceSpace');
    });
});
