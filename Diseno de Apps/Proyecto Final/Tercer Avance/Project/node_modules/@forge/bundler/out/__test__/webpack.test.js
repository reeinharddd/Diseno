"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const webpack_1 = require("../webpack");
describe('webpack', () => {
    describe('getDevToolConfig', () => {
        it('should use source-map config for deployed apps', () => {
            expect(webpack_1.getDevToolConfig({ isWatchMode: false })).toBe('source-map');
        });
        it('should use cheap-source-map config for tunnel apps', () => {
            expect(webpack_1.getDevToolConfig({ isWatchMode: true, isDebugMode: false })).toBe('cheap-source-map');
        });
        it('should use cheap-module-eval-source-map config for tunnel apps in debug mode', () => {
            expect(webpack_1.getDevToolConfig({ isWatchMode: true, isDebugMode: true })).toBe('cheap-module-eval-source-map');
        });
    });
});
